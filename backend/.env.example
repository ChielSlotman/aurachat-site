# Admin secret for code management endpoints
ADMIN_SECRET=changeme

# Token lifetime in ms (default: 1 year)
# TOKEN_LIFETIME_MS=31536000000
PORT=8787
ACCEPT_ANY_CODE=0
# Allowed origins (production):
# ALLOW_ORIGINS=https://chielslotman.github.io,https://api.aurasync.info,chrome-extension://*,http://localhost:3000

# Postgres connection (Render provides DATABASE_URL)
# DATABASE_URL=postgres://user:pass@host:5432/dbname

# Stripe + webhook
# STRIPE_SECRET_KEY=sk_live_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Brevo
# BREVO_API_KEY=your_brevo_api_key
# FROM_EMAIL=you@example.com

# DEV ONLY — skip weekly cooldown in /lost-code
DEV_BYPASS_REGEN_COOLDOWN=false
