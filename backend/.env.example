# Admin secret for code management endpoints
ADMIN_SECRET=changeme

# Token lifetime in ms (default: 1 year)
# TOKEN_LIFETIME_MS=31536000000
PORT=8787
ACCEPT_ANY_CODE=0
# Allowed origins (production):
# ALLOW_ORIGINS=https://chielslotman.github.io,chrome-extension://YOUR_EXTENSION_ID,http://localhost:3000
