Place your database CA certificate here as db-ca.pem if you want strict SSL verification.
Alternatively set PG_CA_BUNDLE env with the PEM contents.
For temporary testing on Render, you can set PG_SSL_INSECURE=true (not recommended for production).
